# Интеллектуальный тьютор на базе LLM для СПО

**Конкурсная заявка платформы Qubu**

Система автоматизации учебного процесса для среднего профессионального образования (СПО) на основе открытых больших языковых моделей. ИИ-тьютор генерирует конспекты лекций, тесты для самопроверки и поддерживает диалоговый режим — освобождая аудиторное время для практических занятий.

| Параметр | Значение |
|----------|----------|
| **Специальность** | 15.02.14 «Оснащение средствами автоматизации технологических процессов и производств» |
| **Организация** | ГБПОУ РО «Сальский индустриальный техникум» (СИТ) |
| **Лицензия** | Apache 2.0 |
| **Команда** | Бардаков Д.Н. (архитектор, ML), Мышанская Н.Г. (методист, контент) |

---

## Проблема

70% аудиторного времени в СПО уходит на изложение теоретического материала. Практические навыки — ключевой показатель employability выпускников — остаются недостаточно отработанными. Преподаватели цикловой комиссии технических дисциплин тратят значительную часть времени на рутину: подготовку конспектов, составление тестов, проверку знаний.

**Гипотеза:** ИИ-тьютор высвободит до 40% аудиторного времени за счёт переноса теории на самостоятельную работу с ИИ-поддержкой, что повысит средний балл аттестаций на 15–20%.

---

## Решение

ИИ-тьютор — on-premise система на базе Mistral Small 24B с QLoRA-адаптацией, интегрированная в Moodle LMS. Три ключевых функции:

1. **Генерация конспектов** — структурированный конспект из текста лекции с ключевыми терминами
2. **Тесты для самопроверки** — вопросы с вариантами ответов (easy / medium / hard)
3. **Диалоговый помощник** — чат для уточняющих вопросов по материалу

**Ключевые преимущества:**
- Данные студентов не покидают периметр техникума (ФЗ-152)
- GPU-агностичная архитектура (CUDA / ROCm)
- Модульные QLoRA-адаптеры — адаптация под любую специальность без переобучения базы

---

## Модель

### Базовая модель

| Характеристика | Значение |
|----------------|----------|
| Модель | Mistral Small 24B-Instruct-2501 |
| Параметры | 24 млрд |
| Контекстное окно | 128K токенов |
| Лицензия | Apache 2.0 |
| Квантование | 4-bit NF4 (QLoRA) |
| Требования GPU | от 48 ГБ VRAM |

### Стратегия дообучения

```
Базовая модель (24B, fp16)
    ↓ QLoRA 4-bit (NF4)
    ↓ r=16, lora_alpha=32
    ↓ 7 target modules (q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj)
    ↓ Gradient checkpointing
    ↓ Chat template: <|im_start|>user / assistant<|im_end|>
Адаптер СПО (специализация 15.02.14)
```

**Три режима обучения:**
- `debug` — 10 шагов, 1 батч, max_seq_length=512 (тестирование пайплайна)
- `light` — 1 эпоха, batch_size=1, gradient_accumulation=4 (быстрая проверка)
- `full` — 3 эпохи, full batch, warmup + cosine schedule (полное обучение)

---

## Датасет

### Характеристики

| Параметр | Значение |
|----------|----------|
| Общий объём | **771 запись** |
| Train / Val / Test | 613 / 73 / 85 (80% / 10% / 10%) |
| Дисциплины | Промышленная автоматизация (ПА) + Измерительные системы (ИС) |
| Формат | JSONL (chat template: system / user / assistant) |
| Язык | Русский |

### Типы задач

| Тип | Количество | Описание |
|-----|-----------|----------|
| `summary` | Конспекты лекций | Структурированное изложение с ключевыми терминами |
| `quiz` | Тестовые вопросы | MCQ с вариантами A/B/C/D, ключи ответов |
| `chat` | Диалоговые объяснения | Ответы на уточняющие вопросы по теме |

### Источники

- **40 лекций** по промышленной автоматизации (30 базовых + 10 LLM-расширенных)
- **27 лекций** по измерительным системам (19 базовых + 8 LLM-расширенных)
- Пайплайн подготовки: `prepare_dataset.py` → `clean_data.py` → `normalize.py` → `split_dataset.py`

### Запуск дообучения на Qubu

```bash
# 1. Установить зависимости
pip install -r requirements.txt

# 2. Подготовить датасет
cd dataset
python prepare_dataset.py    # Markdown → JSONL
python clean_data.py         # Очистка
python normalize.py          # Нормализация
python split_dataset.py      # Train/Val/Test

# 3. Запустить обучение (debug-режим для проверки)
cd ..
python train.py --mode debug

# 4. Полное обучение
python train.py --mode full
```

---

## Архитектура

```
Студент (Moodle) → FastAPI → Celery + Redis → IntelligentTutor → LLM (GPU)
```

- **FastAPI** — 10 эндпоинтов (sync + async: submit → poll → result)
- **Celery + Redis** — очередь задач, singleton-модель в worker
- **IntelligentTutor** — загрузка модели, генерация, chat template
- **Moodle Plugin** — блок «ИИ Тьютор» с AJAX-интеграцией

---

## Оценка качества

### Автоматическая (evaluate.py)

- Perplexity на тестовой выборке
- BLEU / ROUGE (краткий реферат)
- Semantic similarity (embedding-based)

### Ручная (Gold Standard)

Планируется 50 примеров ручной разметки по 6 критериям:
1. Терминологическая точность (1–5)
2. Стиль изложения (соответствие СПО)
3. Полнота покрытия
4. Корректность фактов
5. Практическая применимость
6. Межаннотаторное согласие (Cohen's Kappa ≥ 0.8)

---

## Структура репозитория

```
├── tutor.py              # Основной модуль ИИ-тьютора
├── train.py              # Дообучение (SFTTrainer, QLoRA)
├── evaluate.py           # Оценка качества
├── lora_config.py        # 3 пресета QLoRA (debug/light/full)
├── data_collator.py      # SFT Data Collator с chat template
├── api/                  # FastAPI сервер (10 эндпоинтов)
├── moodle/               # Moodle-плагин (Local Plugin + Block)
├── dataset/
│   ├── raw/              # 67 лекций (Markdown)
│   ├── data/             # JSONL сплиты (генерируется)
│   └── prepare_dataset.py, clean_data.py, split_dataset.py
├── Dockerfile            # Multi-stage GPU-agnostic
├── docker-compose.yml    # Production: api + celery + redis
├── requirements.txt
├── Makefile
└── LICENSE               # Apache 2.0
```

---

## Текущий статус

| Этап | Прогресс |
|------|----------|
| Прототип (M1) | ✅ Завершён |
| Инфраструктура (M2) | 🔶 Сервер ожидается Q2 2026 |
| Дообучение (M3) | 🔶 Датасет готов, скрипты готовы |
| Пилот (M4) | ❌ Q4 2026 |
| Продакшен (M5) | ❌ Q1 2027 |

**Общий прогресс: 56% (65 / 117 задач).**

---

## Риски и митигация

| Риск | Митигация |
|------|-----------|
| Галлюцинации модели | Верификация преподавателем + температурный контроль |
| Задержка закупки GPU | План B: облачный GPU на период обучения |
| Малый объём датасета | 771 запись + синтетическое расширение |
| Низкая ИТ-грамотность | Инструкция для студентов, семинар для педагогов |

---

## Команда

| Участник | Роль |
|----------|------|
| **Бардаков Д.Н.** | Руководитель, архитектор, ML-инженер |
| **Мышанская Н.Г.** | Методист, контент, датасет |

**Организация:** ГБПОУ РО «Сальский индустриальный техникум»
